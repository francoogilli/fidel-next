export interface Contact {
    firstname: string;
    email: string;
    phone: string;
    publicidad: string;
    text: string;
  }
  
  export interface PostRequest {
    properties: Contact;
  }
  